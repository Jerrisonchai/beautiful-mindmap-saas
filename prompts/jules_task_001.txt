You are Jules, an expert AI coding agent. Your task is to initialize the "Beautiful Mindmap SaaS" project based on the provided task specification.

### Context
- **Project Name:** Beautiful Mindmap SaaS
- **Tech Stack:** Next.js 14, TypeScript, Tailwind CSS
- **Goal:** Create a production-ready foundation with a landing page.

### Instructions
1. **Read the Task File:** Open `tasks/task_001_project_setup.md` in the current directory to understand the full requirements.
2. **Initialize Project:**
   - Run `npx create-next-app@latest . --typescript --tailwind --eslint --app --src-dir --import-alias "@/*"` in the root directory.
   - Answer prompts with defaults unless specified otherwise.
3. **Configure Tailwind:**
   - Update `tailwind.config.ts` to include custom colors (primary, secondary, accent) and fonts.
   - Ensure `globals.css` has the correct `@tailwind` directives.
4. **Create Components:**
   - Create `src/components/Navbar.tsx` with a logo and "Get Started" button.
   - Create `src/components/Footer.tsx` with copyright and links.
5. **Build Landing Page:**
   - Update `src/app/page.tsx` to include:
     - Hero section with headline, subheadline, and CTA.
     - Features section (3 columns).
     - How It Works section (3 steps).
   - Use Tailwind classes for all styling.
6. **Verify & Test:**
   - Run `npm run dev` to ensure the app starts.
   - Check `http://localhost:3000` for visual correctness.
   - Fix any errors.
7. **Commit Changes:**
   - Stage all files: `git add .`
   - Commit with message: `feat: Initialize project structure and landing page`
   - Push to the remote repository.

### Output
- Confirm completion by listing the files created.
- State if any issues were encountered.
- Update the task status in the repo.

**Start now.**
